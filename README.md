Best model for dual 3090.

For windows and linux. Great for coding agents like OpenCode, Cline...

Just run the docker compose. vllm with download and start the server.

On linux run vllm directly without docker.